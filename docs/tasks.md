# 작업 계획

> 출시 형태는 **안드로이드 앱(Capacitor)**이다(2026-09-21 확정, decisions.md). 입력 담당 웹 배포는 접고,
> `apps/result`와 소개·정책 페이지만 웹으로 남긴다. M0~M5의 화면 코드는 그대로 앱 화면이 된다.
>
> 한 번에 한 마일스톤씩 진행한다. 각 마일스톤은 계획 → 승인 → 구현 → 테스트 → 원칙 점검 → 커밋 순서.
> 완료한 항목은 `[x]`로 바꾼다.

## 확정한 해석

기획서에서 명시가 부족한 부분을 아래처럼 해석했고, M1 시작 전에 사람이 검토해 확정했다(2026-09-16).
13~17번은 M1 구현 중 스펙에 빈칸이 있어 확정한 것이다. 바꾸려면 기준값 테스트부터 고친다.

| # | 항목 | 이 계획의 해석 |
|---|---|---|
| 1 | 금액 반올림 | 사람별 분담액은 원 단위 반올림. 반올림 차이는 해당 구간 운전자 몫에서 흡수 |
| 2 | 난이도 계수 적용 단위 | 구간별로 계산해 그 구간 노동비에 곱함 |
| 3 | 연속 운전 | 운전자별로 계산. 운전 교대나 15분 이상 휴식이면 초기화 |
| 4 | 조수석 수면 점수 | 누적 분 ÷ 20의 내림, 구간당 최대 +5 |
| 5 | 괘씸 점수 상한 | 구간·사람별 점수 0~10, 배수 = 1 + 0.1 × 점수 |
| 6 | 택시모드와 괘씸 배수 | 택시모드 구간에는 괘씸 배수를 추가로 곱하지 않음(상한 2.0 유지) |
| 7 | 체감 점수 | 5단계 1~5 → −0.1, −0.05, 0, +0.05, +0.1. 도착 요약 3단계 칩은 1·3·5로 저장 |
| 8 | 구간 거리 | 하차·합류 장소 미선택 시 여행 거리 × 구간 운전 시간 비율. 장소 선택 시 재조회 거리 |
| 9 | 통행료 배분 | 결제 기록 금액 우선, 없으면 경로 통행료를 구간 거리 비율로 배분 |
| 10 | 유가 적용 | 주유 기록 전 구간은 조회 평균가, 주유 기록 이후 구간은 주유 단가(정밀 모드 제외) |
| 11 | 최소 송금 목록 | 차액이 큰 채무자부터 큰 채권자에게 순서대로 배정하는 탐욕 방식 |
| 12 | 주유 결제와 공통비 | **확정.** 공통비는 구간 유류비 **계산값**으로 나누고, 주유 결제 금액은 결제자의 “낸 돈”으로만 반영한다. 실제 주유 금액(예: 49,500원)은 탱크에 남는 기름까지 포함해 여행 유류비와 다르므로 분담 대상으로 쓰지 않는다. 파생 규칙: 결제 항목별 “낸 돈”은 그 항목의 공통비 계산값을 넘지 않는 만큼만 정산에 반영하고, 초과분(위 예시의 49,500 − 이번 여행 유류비 몫)은 결제자 자기 부담이다. 초과분을 정산하면 동승자가 탱크에 남는 기름값까지 내게 되고 차액 합계 0이 깨진다. 영수증 계산 근거에 “주유 49,500원 중 이번 여행 몫 ○○원”으로 표시한다 |
| 13 | 구간별 예상 시간 | 전체 예상 시간을 구간 거리 비율로 배분. 하차·합류 장소를 골라 재조회한 구간은 그 조회의 예상 시간을 쓴다. 난이도 정체 요소에만 쓰인다 |
| 14 | 재분배형 공식 | i의 분담 = 구간 노동비 × (i의 배수 ÷ 비운전 탑승자 배수 합). 노동비 총액은 고정이고 괘씸한 사람 비중만 커진다. 반올림 차이는 운전자 몫에서 흡수하므로 총액이 1원까지 벌어질 수 있다 |
| 15 | 구간 통행료 우선순위 | ① 재조회한 구간 통행료 ② 통행료 결제 기록 합계를 구간 거리 비율로 배분 ③ 여행 경로 통행료를 구간 거리 비율로 배분. 해석 9를 이 순서로 읽는다 |
| 16 | 주유 단가 적용 경계 | 구간 종료 시각까지의 마지막 주유 기록 단가를 그 구간에 쓴다. 주유는 보통 구간 경계의 휴식 중에 일어난다 |
| 17 | 난이도 반올림 시점 | 요소는 원값으로 합산하고 계수만 소수 둘째 자리에서 반올림한다. 영수증에 찍는 요소별 값만 각각 반올림하므로 요소 합과 계수가 1/100 다를 수 있다 |
| 18 | 결제 기록 없는 공통비 | 결제 기록으로 덮이지 않은 공통비는 운전자(차주)가 냈다고 본다. 그렇게 하지 않으면 낸 사람이 없어 차액 합계가 0이 되지 않는다. spec-screens.md S10b 계산 근거의 "민수 40,000"이 이 경우다. `commonPaidBy: 'none'`으로 끌 수 있다 |

## M0 · 모노레포 뼈대

- [x] pnpm 워크스페이스, TypeScript strict, ESLint·Prettier
- [x] `packages/calc`, `packages/link-codec`, `packages/storage` 빈 패키지와 Vitest 설정
- [x] `apps/web`(Vite + React, PWA 매니페스트·서비스 워커), `apps/result`(Vite), `apps/worker`(Wrangler)
- [x] 루트 스크립트: `test`, `dev:web`, `dev:result`, `dev:worker`, `build`
- [x] Worker가 빌드된 web을 `/`, result를 `/r`로 서빙하는 로컬 구성
- [x] CLAUDE.md의 "명령어" 섹션을 실제 명령으로 갱신

**완료 기준:** `pnpm test`가 빈 테스트로 통과, `pnpm dev:worker`로 로컬에서 `/`와 `/r`이 열림.

## M1 · 계산 엔진 (`packages/calc`)

- [x] 도메인 타입: Trip, Member, TripEvent, Segment, PaymentRecord, PenaltyEvent, PriceSnapshot
- [x] TripEvent → Segment 자동 생성 (하차·합류·운전 교대·휴식)
- [x] 유류비, 통행료 배분, 공통비 분담
- [x] 노동비: 시급형, 택시형, 난이도 계수, 택시모드 범위 자르기
- [x] 괘씸·감면 점수 → 배수 (가산형 기본, 재분배형 옵션)
- [x] 사람별 낸 돈·몫·차액, 최소 송금 목록
- [x] 택시 대비 아낀 돈

**완료 기준:** 아래 기준값 테스트가 모두 통과. 기준값 테스트는 이후 어떤 작업에서도 삭제하거나 기대값을 바꾸지 않는다.

### 기준값 테스트 A · 강릉 여행 (spec-calc.md "계산 예시")

입력
- 멤버: 민수(차주·운전), 지현, 태호, 수빈. 시급형 10,320원, 연비 12km/L, 가산형
- 이벤트: 09:00 출발(4명) → 10:30~10:40 휴식 → 10:36 수빈 하차(장소: 원주휴게소 선택) → 10:38 주유 30L × 1,650원, 민수 결제 → 11:40 도착
- 구간 ① 서울→원주: 120km, 운전 90분, 통행료 5,000원, 유가 조회 1,700원/L, 택시 140,000원, 난이도 1.0
- 구간 ② 원주→강릉: 108km, 운전 60분, 통행료 3,150원, 유가 1,650원/L, 택시 125,000원, 난이도 1.0
- 괘씸: ① 지현 조수석 수면 60분(+3), 태호 혼자 먹음(+1) / ② 태호 시끄러움 3회(+3), 지현 기타 1회(용서함 → 0)
- 낸 돈: 이 예시는 공통비 40,000원(구간 유류비 계산값 + 통행료)을 민수가 모두 냈다고 본다. 주유 결제 금액(49,500원)을 쓰는 경우는 해석 12에 따라 기준값 테스트 E에서 따로 본다

기대값
| 항목 | 값 |
|---|---|
| ① 유류비 / 공통비 / 1인 공통비 | 17,000 / 22,000 / 5,500 |
| ② 유류비 / 공통비 / 1인 공통비 | 14,850 / 18,000 / 6,000 |
| ① 노동비 / 1인 기본 분담 | 15,480 / 5,160 |
| ② 노동비 / 1인 기본 분담 | 10,320 / 5,160 |
| 지현 보낼 돈 | 23,368 |
| 태호 보낼 돈 | 23,884 |
| 수빈 보낼 돈 | 10,660 |
| 민수 받을 돈 | 57,912 (공통비 회수 28,500 + 노동비 29,412) |
| 택시 대비 아낀 돈 | 195,588 (265,000 − 69,412) |
| 차액 합계 | 0 |

> 참고: 이 기준값은 해석 12의 결정과 무관하게 유지한다(낸 돈 = 공통비 40,000원 가정).

### 기준값 테스트 E · 주유 결제와 공통비 (해석 12)

테스트 A와 같은 입력에서 민수의 낸 돈을 주유 결제 30L × 1,650원 = 49,500원 + 통행료 8,150원으로 바꾼다.

- [x] 구간 ② 유류비는 주유 실결제액이 아니라 계산값 14,850원(9L × 1,650원)이다
- [x] 1인 공통비는 테스트 A와 같다 (① 5,500 / ② 6,000)
- [x] 주유 결제 49,500원 중 이번 여행 유류비 몫을 넘는 초과분은 민수 자기 부담이고, 동승자 4명의 보낼 돈은 테스트 A와 같다
- [x] 차액 합계는 0이다

### 기준값 테스트 B · 돌아오는 밤길 (난이도)

입력: 강릉→서울, 20:30 출발, 휴식 없이 00:10 도착(운전 220분), 예상 160분, 비, 체감 4점, 도로 유형 0
기대값: 정체 +0.19, 야간 +0.18(야간 130분), 연속 +0.09(초과 100분), 기상 +0.20, 체감 +0.05 → **계수 1.71**, 노동비 **64,706원** (난이도 없이 37,840원)

### 기준값 테스트 C · 택시모드

입력: 서울→원주 2인(민수 운전, 지현 뒷자리 표시), 택시 140,000원, 공통비 22,000원, 운전 90분, 난이도 1.0
기대값: 택시형 금액 35,400원 → 범위 [23,220, 30,960]로 잘라 **30,960원**

### 기준값 테스트 D · 규칙

- [x] 괘씸 배수는 2.0을 넘지 않는다 (점수 15 입력 → 배수 2.0)
- [x] 감면으로 점수가 0 미만이 되지 않는다
- [x] 기타 항목은 구간당 2회까지만 점수 반영
- [x] 난이도 계수는 1.0 미만, 2.0 초과가 되지 않는다
- [x] 하차 장소 미선택: 여행 228km, 구간 운전 90분·60분 → 구간 거리 136.8km·91.2km
- [x] 사람 3명이 10,000원을 나누면 3,333 + 3,333 + 3,334처럼 합계가 보존된다(차이는 운전자 몫)

## M2 · 필수 화면 (`apps/web`)

- [x] S1 홈, S2 새 여행, S6 여행 중 기록, S9 도착 요약, S10 정산 결과(말투 포함)
- [x] `packages/storage` IndexedDB 구현, 모든 입력 즉시 자동 저장, 새로고침해도 진행 중 여행 유지
- [x] 경로 API 없이 동작: 거리·통행료를 임시 수동 값으로 넣는 개발용 스텁
- [x] 야간 모드 토큰(주간·야간) 적용, 주요 숫자 24px 이상, 터치 영역 48px 이상

> M2에서 S7 결제 기록의 축소판(종류·금액·결제한 사람·주유 단가)을 함께 만들었다.
> 낸 돈이 없으면 기준값 테스트 A를 화면으로 재현할 수 없다. 주유소 검색과 정밀 모드는 M5에 남는다.

**완료 기준:** 브라우저에서 S2 → S6 → S9 → S10 필수 탭 약 5회로 정산 결과가 나오고, 기준값 테스트 A의 입력을 화면으로 재현하면 같은 금액이 나온다.

## M3 · Worker (`apps/worker`)

- [x] `POST /api/session` Turnstile 검증(테스트 키) → HMAC 세션 토큰
- [x] 요청 검증 순서: 입력 검증 → 토큰 → 차단 목록 → 분당 제한 → 캐시 → 일일 예산(DO) → 외부 호출
- [x] `POST /api/route` 카카오 주력·TMAP 예비 어댑터, 응답 숫자 6개로 축소, 좌표 해시 캐시 키
- [x] `GET /api/places` 검색어만 받음, 상위 5개·이름·주소·좌표만
- [x] `GET /api/fuel/avg` KV 읽기 전용, Cron이 오피넷 평균가 갱신
- [x] Durable Object 일일·시간당 하드 상한 (카카오 경로 9,000 / 장소 50,000, TMAP 900 / 18,000)
- [x] 비상 스위치(KV), 사용률 알림 Cron(웹훅 URL은 secret)
- [x] 모든 외부 호출은 fixtures 기반 테스트

**완료 기준:** fixtures로 경로·장소·유가 API 테스트 통과, 상한 도달 시 503과 앱의 수동 입력 전환 확인, 로그에 요청 본문·좌표·검색어가 없음.

> 완료(2026-09-18). 계획 단계에서 확정한 결정 8개는 decisions.md "M3 Worker 결정"에 있다.
> 앱 연결(S2·S3 장소 검색, 출발 시 경로·유가 조회, S9c 하차 장소 재조회, 한도 시 수동 입력)도 M3에 포함했다.
> 로컬 개발은 `pnpm dev:worker`(fixtures 모드). 실제 키·KV·Turnstile 위젯·secret 등록은 M6.

## M4 · 결과 링크 (`packages/link-codec`, `apps/result`)

- [x] 결과 JSON → deflate → base64url, `/r#v1.` 형식
- [x] 스키마 검증: 4KB·해제 32KB 상한, 멤버 10, 구간 10, 이름 10자, 금액 0~5,000,000, 송금 링크 허용 형식
- [x] S11: 사람별 금액 먼저, 이름 탭 시 근거 펼침, 고정 안내 문구, CSP로 외부 스크립트 차단
- [x] Web Share API 공유 본문(말투별), 미지원 시 복사

**완료 기준:** 정상·손상·위조(스크립트 삽입, 계좌번호, 허용 외 URL, 압축 폭탄) 링크 테스트 통과.

## M5 · 선택 화면과 마감

- [x] S3 빠른 정산, S6a 기타 괘씸, S7 결제 기록 (S8 운전 중 잠금은 남음)
- [x] S9a 괘씸 보기, S9b 난이도 상세, S9c 타임라인, S10b 계산 근거, S10c 고치기
- [x] 야간 모드 토큰, 기본값/자동/직접 입력 태그, 조회 한도 안내(M3)
- [x] ~~광고 카드 컴포넌트~~ → 광고를 넣지 않기로 확정(2026-09-21). M10에서 자리표시 카드를 지운다

> 남은 것: **S8 운전 중 잠금**. 브라우저에서는 화면이 꺼지면 속도 감지가 멈춰 만들 수 없었다.
> 앱 전환 후 M10에서 만든다.
> S9c의 하차 장소 검색과 경로 재조회는 M3에서 연결했다.

**완료 기준:** spec-screens.md 화면별 동작 규칙 체크리스트 통과.

## M6 · 배포와 공개 페이지

- [x] 소개·사용 가이드·계산 방법·개인정보처리방침 페이지(`apps/site`, 외부 스크립트 없음)
- [x] Worker `/`를 앱 화면 대신 소개·정책 페이지로 바꾸기(`/`, `/guide`, `/calc`, `/privacy`. `/r`은 그대로)
- [ ] workers.dev 배포 **(사람 승인 필요)**. 방침 URL과 앱의 API·결과 링크 주소가 여기서 확정된다. secret 없이 배포해도 정적 페이지는 뜨고 `/api`만 503이다
- [ ] secret 등록, 실제 키로 소량 통합 테스트 **(사람 승인 필요)**
- [ ] 배포 주소가 정해지면 `.env.app.local`의 `VITE_API_BASE`·`VITE_RESULT_BASE`, Worker `ALLOWED_ORIGINS`, Turnstile 위젯 호스트, 개인정보처리방침 URL을 채운다
- [ ] 개인정보처리방침의 시행일·문의 이메일 채우기
- [ ] decisions.md "남은 확인 사항" 중 서버·법률 항목 확인

**완료 기준:** 배포한 Worker에서 `/`(소개·정책)와 `/r`(결과 보기)이 열리고, 실제 키로 경로·장소·유가 조회가 한 번씩 성공한다.

> 페이지 뼈대는 완료(2026-09-21). 로컬 `wrangler dev`에서 `/`, `/guide`, `/calc`, `/privacy`, `/r`이 모두 200이다.
> 광고를 넣지 않기로 해서 "광고 매체 심사 대비"라는 이유는 없어졌다. 이 페이지가 필요한 이유는
> 스토어 심사(개인정보처리방침 URL 필수)와 앱 안에서 거는 정책 링크다.
> 배포·secret·실제 키 테스트는 사람 승인이 필요해 실행하지 않았다.

## M7 · 앱 껍데기 (`apps/mobile`)

- [x] Capacitor 도입(`@capacitor/core`·`cli`·`android`), `apps/mobile`에 설정과 안드로이드 프로젝트
- [x] 앱 ID `kr.driverlabor.app`, 앱 이름 "오늘 운전 완료", 아이콘·스플래시 자리표시
- [x] `apps/web` 빌드 결과를 앱 자산으로 복사하는 스크립트와 pnpm 명령(`sync`, `apk`, `install:emulator`)
- [x] 서비스 워커(PWA 플러그인)를 앱 빌드에서 빼기. 앱은 자산을 내장한다
- [x] 에뮬레이터(`fearless_test`, Pixel 6, Android 15)에서 S1~S11 화면이 뜨는지 확인. 빌드는 JDK 21
- [x] 안드로이드 뒤로 가기 버튼 처리(화면 스택), 상태바·노치 여백

**완료 기준:** 에뮬레이터에 설치한 앱에서 새 여행 → 기록 → 도착 → 정산 → 결과 링크 만들기까지 막히지 않는다. 자동 조회는 아직 안 돼도 된다.

> 완료(2026-09-21). 에뮬레이터에서 새 여행 → 기록 → 도착 → 정산 → 결과 링크 공유까지 확인했다.
> 확인 중 찾은 것 두 가지를 같이 고쳤다: 출발과 같은 분에 "도착했어요"를 누르면 구간을 만들지 못해
> 화면이 멈추던 문제(최소 1분), 응답이 JSON이 아닐 때 조회가 처리되지 않은 오류로 끝나던 문제.
> 결과 링크가 아직 `https://localhost/r#…`이라 M8에서 배포 주소로 바꾼다.

## M8 · 앱과 서버 연결

- [x] API 주소를 빌드 환경 변수(배포 주소)로. 앱에서는 절대 주소로 호출
- [x] Worker `/api`에 CORS 허용 목록(`https://localhost`, 개발 주소)과 `OPTIONS` 응답
- [x] 결과 링크 주소를 `location.origin` 대신 배포 주소 상수로
- [x] Turnstile을 WebView에서 확인(위젯 호스트 등록은 사람 작업), 실패 시 수동 입력 전환이 그대로 도는지
- [x] 네이티브 공유 시트(`@capacitor/share`)로 결과 링크 공유, 미지원 시 복사

**완료 기준:** 에뮬레이터에서 장소 검색·경로·유가 조회가 모두 성공하고, 한도(503)일 때 수동 입력으로 넘어간다. 공유 시트에서 카카오톡 등으로 링크가 간다.

> 완료(2026-09-21). 에뮬레이터 + 로컬 Worker(fixtures)로 확인했다.
> 장소 검색 → 경로·유가 조회 → 결과의 "조회: 카카오 · 유가 1,694원/L (강원 평균)" 표시,
> 비상 스위치 `auto_route=off`로 만든 503에서 "오늘 자동 조회 한도에 닿았어요" 안내와
> 거리·통행료 기본값 전환, 안드로이드 공유 시트에 설정한 주소의 결과 링크.
> Turnstile은 공개 테스트 sitekey로 WebView에서 토큰이 발급됐다. 실제 위젯 등록은 배포(M6) 때.
>
> **평문 예외 확인:** 개발 빌드에서만 연다. 릴리스 APK(`app-release-unsigned.apk`)의
> `assets/capacitor.config.json`은 `allowMixedContent: false`였고, 릴리스 병합 매니페스트에는
> `networkSecurityConfig`·`usesCleartextTraffic`이 없었다(디버그 매니페스트에만 10.0.2.2용 설정).
> 확인 명령은 architecture.md "운영 메모"에 적었다.

## M9 · 앱 관례 맞추기

- [x] 권한·개인정보 고지 화면(첫 실행 1회, 위치가 폰 밖으로 나가지 않는다는 설명)
- [x] 저장소 확인: WebView IndexedDB에 여행이 남는지, 앱 재설치 시 사라진다는 안내
- [x] 야간 모드가 앱 테마·상태바에도 적용
- [x] 앱 이름·버전·빌드 번호 관리, 디버그/릴리스 빌드 구분

**완료 기준:** 첫 실행 안내가 한 번만 뜨고, 앱을 껐다 켜도 진행 중 여행이 그대로다.

> 완료(2026-09-21). 에뮬레이터 확인:
> - 앱 데이터 삭제(`pm clear`) 뒤 첫 실행에 고지가 뜨고, 확인 후 앱을 껐다 켜면 다시 뜨지 않는다.
> - APK를 덮어 설치(`install -r`)해도 저장한 설정·여행이 남는다. 앱을 지우거나 데이터를 지우면 사라진다.
>   그래서 안내 문구는 "앱을 지우면 사라집니다"로 적었다.
> - 폰 다크 모드로 바꾸면 화면이 검정으로, 상태바 글자도 배경에 맞춰 보인다.
> - 홈 맨 아래 버전 표시가 `apps/mobile/version.json`과 같다(v1.0.0 (1), APK versionName 1.0.0 / versionCode 1).
> - 릴리스 빌드 평문 예외 재확인: 릴리스 APK `allowMixedContent: false`, 병합 매니페스트에
>   `networkSecurityConfig`·`usesCleartextTraffic` 없음.
>
> **화면 꺼짐 방지는 M10으로 미뤘다.** 운전 중 화면을 계속 켜 두는 것이 운전 중 잠금(S8)의 취지와
> 부딪힐 수 있어, 잠금 화면 설계와 함께 정한다.

## M10 · 시작·완료(미터기)와 도착 후 입력

> 2026-09-21 사용자 결정: 출발할 때 "시작", 도착해서 "완료"를 누르는 미터기 방식이 기본이다.
> 운전 중에는 화면을 보지 않고, 위치·속도는 쓰지 않는다. 주행 중 기록 화면(옛 S6)과
> 운전 중 잠금(옛 S8)은 삭제한다. 광고도 넣지 않기로 확정돼 결과 화면의 자리표시 광고 카드를 지운다.
> 마일스톤 번호는 그대로 둔다(M11은 삭제, M12 스토어).

- [x] 홈(S1): 큰 "시작" 버튼, 진행 중이면 "완료"와 경과 시간, 작은 "지난 여행 입력"
- [x] S2 출발: 시작 시각만 기록하고 바로 홈으로. 즉시 저장해 앱이 꺼져도 남게
- [x] S2b 도착 시각 확인: 시작 후 6시간이 지나 완료를 누르면 "언제 도착했어요?"를 한 번 묻는다. 자동으로 자르지 않는다
- [x] S9 도착 요약에서 도착지·인원을 받고 경로·유가를 1회 조회
- [x] S9a 괘씸 입력·확인: 구간·대상 선택, 타일 +1/−1(구간·사람당 2회), 조수석 수면 분 입력, 기타(S9d)
- [x] 결제(S7)·하차·합류·교대·휴식을 타임라인(S9c)과 고치기(S10c)에서 넣도록 이동
- [x] S3를 "지난 여행 입력"으로. 여기서 온 운전 시간에는 "직접 입력" 표시
- [x] S2 새 여행·S6 계열 화면과 수면 타이머·휴식 토글 삭제, 여행 상태를 진행 중/완료로 정리
- [x] 도착 시각 검증을 "도착 > 출발"로
- [x] 결과 화면의 자리표시 광고 카드 삭제(광고 없음 확정)

**완료 기준:** 시작 → (앱을 닫아도 됨) → 완료 → 도착지·인원 입력 → 정산 결과 → 결과 링크까지 간다. 앱을 강제 종료하거나 하루를 넘겨도 시작 시각이 남아 있고, 늦은 완료는 도착 시각을 물어 계산한다. 괘씸·결제·하차를 도착 후에 넣어도 기존 기준값 테스트와 같은 금액이 나온다. 삭제한 화면을 가리키는 코드·문서가 남아 있지 않다.

> 완료(2026-09-21). 에뮬레이터(fearless_test) + 로컬 Worker(fixtures)로 확인했다.
> - 홈 "시작" → 출발 화면(시작 시각) → 홈에 "진행 중 · 경과 시간"과 "완료" 버튼
> - "완료" → 도착 요약에서 도착지 검색·선택(장소 목록 5개), 인원 2명, 운전 시간 자동
> - 괘씸 화면에서 타일 +1과 조수석 수면 60분 입력 → 결과 영수증에 사유 두 줄 반영
> - 정산 결과에 광고 카드 없음, 야간 모드에서 검정 배경·대비 확인
> - **S2b는 기기 시간을 돌리지 않고 확인했다.** `ASK_ARRIVAL_AFTER_MS`를 0으로 둔 임시 빌드를
>   설치해 화면(출발 시각 고정 표시, 도착 날짜·시각 입력, "직접 입력" 안내)을 보고 되돌렸다.
>   6시간·24시간 판정과 "도착 > 출발" 방어는 단위 테스트로 덮여 있다.
> - 확인 중 찾은 것: 시작 직후 완료를 누르면 여행 길이가 0이라 구간을 만들 수 없다.
>   앱이 잰 값이므로 묻지 않고 최소 1분으로 본다(`arrivalFor`).
> - 타임라인의 결제·휴식 입력은 이번 확인에서 화면까지 열어 보지 못했다(정산 완료 후에는
>   도착 요약을 다시 열 수 없어서). 다음 에뮬레이터 차례에 본다.

> M11(AdMob 광고)은 삭제했다. 광고를 넣지 않기로 확정(2026-09-21). 수요가 보이면 그때 다시 검토한다.
> 마일스톤 번호는 그대로 둔다(M12 스토어 출시).

## M12 · 스토어 출시

> 준비 목록·문구 초안·데이터 보안 답변은 `docs/store-release.md`에 있다. 기본 경로는 개인 계정 기준이다.

- [ ] 업로드 키스토어 생성·보관(사람), release 서명 설정과 `.gitignore`(코드). Play 앱 서명을 켠다
- [ ] 아이콘 512×512, 그래픽 이미지 1024×500, 스크린샷 4장, 짧은·자세한 설명(초안 있음)
- [ ] 콘텐츠 등급 설문, 카테고리, “광고 없음” 표시
- [ ] 데이터 보안 양식(위치·광고 식별자 없음. 검색어·좌표 전송을 어떻게 신고할지 사람이 확인)
- [ ] 개인정보처리방침 페이지를 M6에서 올리고 URL 연결
- [ ] 릴리스 AAB 빌드 → 내부 테스트 트랙 업로드 → 실기기 확인
- [x] Play 개발자 계정 등록(2026-09-21 완료, **개인 계정**)
- [ ] 클로즈드 테스트 12명 × 14일 연속 진행, 피드백 반영. 테스터는 세 앱에 같은 사람을 쓰므로 세 앱을 같은 시점에 시작한다
- [ ] 프로덕션 신청

**완료 기준:** 내부 테스트 트랙에서 실기기 설치·정산·공유가 되고, 클로즈드 테스트 12명 × 14일을 채운다.

> 당장의 목표는 **내부 테스트 트랙에 올릴 수 있는 상태**다. 필요한 것과 미뤄도 되는 것은 `docs/store-release.md` 0장에 나눠 적었다.

> M12 전에 끝낼 것: 타임라인의 결제·휴식 입력 화면 확인(M10에서 못 봄), M6 배포·secret·실제 키 테스트,
> Turnstile 위젯 호스트 등록, targetSdk 확인, 계정 유형 확인.

## 마일스톤 프롬프트 예시

```
docs/tasks.md의 M1을 진행하자. CLAUDE.md와 docs/spec-calc.md를 읽고
기준값 테스트 A~E를 먼저 테스트 코드로 작성한 뒤(실패 확인), 구현 계획을 보여줘.
해석 12는 확정됐으니 표에 적힌 대로 구현한다.
```

```
M1 끝났으면 packages/calc 밖에서 금액을 계산하는 코드가 없는지,
기준값 테스트가 전부 남아 있는지 점검하고 결과를 요약해줘. 그다음 커밋해줘.
```
